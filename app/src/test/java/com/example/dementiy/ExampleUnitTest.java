package com.example.dementiy;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }

    public void division_inCorrect() {
//        assertEquals(2, MainActivity.divizion(50));
//        assertEquals(1, MainActivity.divizion(51));
//        assertEquals(20, MainActivity.divizion(5));
//        assertEquals(10ч, MainActivity.divizion(40));
    }

}